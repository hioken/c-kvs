# アプリ情報
## 動作確認環境
- ubuntu
  - 24.04

## バージョン・使用ツール
- C言語: C17
- gcc: 13.3.0

## 開発補助ツール
- Gemini: Gemini 3 Deep Think
- Gemini: Gemini 3.1 Pro
- Valgrind: 3.22.0
- Makefile: 4.3

## 使用メモリ
- 約1Mbyte

# 開発仕様
## 動作環境・アーキテクチャ
- 動作形態: 単体動作（ネットワーク非依存のC言語プログラム）

## メモリ管理
- **バッファ設定**
  - 容量: 4096byteの配列を確保
  - 入力制限: 1024byte以下の入力のみ許可（超過時はエラー返却）

## コアアルゴリズム・データ構造
- KV管理方式: メモリプール方式（全データ型をunionを用いた単一の共通構造体で管理）
- ハッシュ関数: FNV-1a
- ハッシュ衝突解決: チェイン法
- 空き領域管理: フリーリスト（Free-list）方式
- データ削除（DEL）処理: 削除対象の領域をフリーリストにポインタ操作で挿入（繋ぎ変え）するのみ

## ディレクトリ
```Plaintext
ディレクトリ構成:
├── bin/
├── obj/
├── src/
│   ├── main.c
│   ├── kvs.c 変更予定
│   ├── kvs.h 変更予定
│   ├── (sysinfo.c)
│   ├── (sysinfo.h)
│   ├── (error.c)
│   └── (error.h)
└── test/
    └── test_main.c
└── sundbox/   消す前提、一時的なテストファイル等
```

## テスト・動作確認方針
- **検証頻度**: 部品（関数・モジュール）の完成ごとに、Valgrindを用いて変数およびメモリの追跡を行いながら動作確認を実施する。
- **テスト環境構成（オブジェクトリンク方式）**: 
  - `test/test_main.c` を作成し、`src/kvs.h` や `src/error.h` などの必要なヘッダーのみをインクルードする。
  - コンパイル時に `obj/kvs.o` などのオブジェクトファイルをリンクしてテストバイナリを生成する。
- **開発手法**: 各ファイルの関数ひな型（スケルトン）を作成した直後に `test_main.c` 側にテストロジックを記述し、半テスト駆動開発（半TDD）のサイクルで実装を進める。
- **assertの利用制限**: `assert` によるアサーションは `test/test_main.c` 内にのみ記述し、それ以外の製品コード（`src/` 配下）では一切使用しない。

# 開発用メモ
## Makefile
| **実行コマンド** | **モード** | **追加されるマクロ** | **実行される動作** |
| :--- | :--- | :--- | :--- |
| `make test` | `test`モード | `-DNOSTATIC` | `test/test_runner` をビルド・実行し、`.o` を削除 |
| `make` (または `make all`) | デバッグモード (デフォルト) | なし | `bin/kvstore` をビルド |
| `make prod` | 本番想定動作確認 | `-DNDEBUG` | `bin/kvstore` をビルド |
| `make release` | リリース | `-DNDEBUG` | `bin/kvstore` をビルド |

## Valgrind
```bash
valgrind --leak-check=full --track-origins=yes
```

## 開発工程・ロードマップ

### 1. デバッグ情報もなしの最低限のget, set作成
1. `string` の `get`, `set` 、衝突解決の `set` のみを想定したひな型の作成
2. `input_line()` に直接引数を渡す形で、`get` と `set` のテストを書く
3. 衝突解決を追加
4. ひな型を埋める・動作テスト

### 2. デバッグ、例外処理追加
1. ディレクトリの () 部分のファイル (`error.c`, `error.h`, `sysinfo.c`, `sysinfo.h`) を作成
2. デバッグ情報を追加
3. 例外処理の追加

### 3. 基礎機能追加
1. `del`, `type`, `exists` のテスト追加
2. 上記の関数追加、テスト、動作確認

### 4. データ型追加の準備
1. READMEの編集
2. `cmd_string.c`, `.h` 及び `test_cmd_string.c` を作成
3. `get`, `set` を上記に移し、テストを行えるように変更

### 5. データ型追加
`string` と同じフォーマットで `Hash`, `List` 追加
1. `Hash` の `HSET`, `HGET` のテスト、実装
2. `List` の `L/RPUSH`, `LINDEX` のテスト、実装

### 6. 一括機能追加
1. `String` に `MGET`, `MSET` 追加

### 7. 動的リサイズ
1. READMEに仕様を定義
2. 動的リサイズのテストを作成 (`test_main.c`)
3. デフォルトのサイズを変更、動的リサイズ実装、テスト、動作確認
4. クラッシュ回避機構を作成
   - 監視方法: `sysinfo` によるOS全体のメモリ使用量監視
   - 制限到達時の挙動: 新規命令の拒否
   - 閾値（本番想定）: 全体メモリの 90%
   - 閾値（テストモード）: 全体メモリの 30%

### 凍結項目
#### 優先度1
- **接続・生存確認コマンド**: `PING`（PONGを返す）や `ECHO`（引数の文字列をそのまま返す）といった、パースや動作検証の基準となる基礎コマンド。
- **SETS, Sorted Set**
- **バイナリへの対応**: 文字列だけでなく、ヌル文字（\0）を含むバイナリデータをキーや値として扱えるようにする対応。
- **柔軟なエラー・制限対応**: 起動オプション等に応じて、エラーハンドリングの挙動やメモリ制限の厳しさを切り替える仕組み。
- **構造体valの最適化**: 全データ型を同一構造体で管理する上で生じる、メモリ空間の無駄（未使用領域）を削減するレイアウト設計。

#### 優先度2
- **データの永続化機構**: AOF（ログへの追記）や RDB（スナップショットのファイル書き出し）などのファイルI/Oを伴う永続化処理。
- **レプリケーション**: 複数プロセス間やネットワークを介したデータ同期・複製機構。
- **トランザクション**: 複数のコマンドをひとまとめにして実行保証する仕組み（MULTI/EXEC等）。
- **Bitmap / HyperLogLog**: ビット単位の操作や、巨大なデータの異なり数を概算する特殊なデータ構造。

## ファイルと関数
### main.c: 入力受付とパース
- main(): kvsのsetup()を実行後、ループ処理でinput_line()を呼び出し続ける
- input_line(): 標準入力を受け付け、内部でparse_input()を呼び出す
- parse_input(): 入力文字列をトークン（引数配列）に分解し、該当するkvsハンドラへ流す
### kvs.c: KVSコア・メモリ管理・ハンドラ一括実装
- グローバル: `static` 変数として、実データプール、ハッシュテーブル、フリーリスト先頭ポインタの実体を定義（メモリの静的確保）
  - テスト環境用にマクロ`NOSTATIC`を使用して分岐で`static`を外す
- setup(): 静的確保された実データプールのデフォルト値セット（フリーリストの初期の接続や初期値代入など）のみを実行
- 各種ハンドラ: main.cから呼ばれる各コマンド（SET, GET, DEL等）の実装
  - *ポインタ操作*: 各ハンドラ内でフリーリストやチェインのnextポインタを直接操作する
### kvs.h: KVSの外部API定義
- `setup()` や各種ハンドラなど、`main.c` から呼び出す関数のプロトタイプ宣言
### error.c / error.h: 例外処理
- 各種例外処理関数
### sysinfo.c / sysinfo.h: システム情報取得
- OS全体のメモリ使用量を監視するためのシステム情報取得関数