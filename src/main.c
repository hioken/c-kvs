#include <stdio.h>
#include <string.h>

int input_line(void)
int parse_input(char*);

typedef struct {
    const char *cmd_name;
    void (*func)(void);
} CommandTable;

int main(void) {
  // kvsのsetup呼び出し
  // ループ
    // if (input_line()) { エラー }
  return 0;
}

int input_line(void) {
  // 入力待ち
  // if sizeチェック エラー
  // if (parse_input(入力)) return 1;
})

int parse_input(char* input) {
  // int flg = 1;
  // 解析
  // static const CommandTable
  // ループ処理
    // 見つかったら flg = 0;
  // if (flg) {エラー return 1}

  return 0;
}